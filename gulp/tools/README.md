# 🛠️ Инструменты сборки Gulp

Эта папка содержит инструменты разработки, которые являются частью системы сборки, а не исходным кодом проекта.

## 📁 Структура

### `control-panel/`
**Control Panel** - интерактивная панель управления проектом через браузер.

- **Расположение**: `gulp/tools/control-panel/control-panel.html`
- **Назначение**: Инструмент управления проектом, доступный через браузер
- **Копирование**: Автоматически копируется в `dist/` при сборке
- **Доступ**: `http://localhost:3000/control-panel.html`

## 🎯 Концепция "Контроллер внутри контроллера"

Control Panel реализует архитектурный паттерн "контроллер внутри контроллера":

- **Внешний контроллер**: `control-panel.html` - интерфейс управления проектом
  - Левая панель: навигация по страницам
  - Правая панель: настройки (библиотеки, цвета, типографика)
  - Верхняя панель: переключение viewport

- **Внутренний контроллер**: `iframe` с превью проекта
  - Отображает реальные страницы проекта (`index.html`, и т.д.)
  - Поддерживает live reload через BrowserSync
  - Может показывать сам control-panel (рекурсивный превью)

## 🔄 Работа через API

Control Panel **не создает файлы напрямую**, а работает через REST API:

1. **Управление конфигурацией**: Изменения сохраняются в `gulp-config.json` через API
2. **Генерация компонентов**: Вызывает gulp задачи через API endpoints
3. **Установка библиотек**: Использует npm через API (`/api/library/:name`)
4. **Изменение viewport**: Обновляет конфигурацию и BrowserSync через API

Все изменения применяются автоматически через систему сборки Gulp.

## 🌐 Глобальное использование

Инструменты в `gulp/tools/` можно скопировать в другие проекты:

1. Скопируйте папку `gulp/tools/` в новый проект
2. Убедитесь, что задача `static` включена в `gulpfile.js`
3. Control Panel автоматически станет доступен в новом проекте

## 📝 Настройка

Control Panel настраивается через:
- `gulp-config.json` - основная конфигурация проекта
- API endpoints в `gulp/tasks/api-server.js`
- Статические настройки в самом `control-panel.html`

---

**Control Panel** - это не страница проекта, а инструмент разработки, интегрированный в процесс сборки.

